# TabBar 图标说明

请在此目录下放置以下图标文件（建议尺寸：81x81 像素）：

- `home.png` - 首页图标（未选中状态）
- `home-active.png` - 首页图标（选中状态）
- `mine.png` - 我的图标（未选中状态）
- `mine-active.png` - 我的图标（选中状态）

如果暂时没有图标，可以在 `src/app.config.ts` 中临时注释掉 `iconPath` 和 `selectedIconPath` 字段，小程序将只显示文字。

